# إصلاح استيراد سلة SHEIN — V10.12.1

## السبب

بعد مقارنة كود مستورد SHEIN مع النسخة التي نجحت سابقًا في قراءة 35 منتجًا، تبيّن أن تغييرًا لاحقًا جعل عامل Playwright يرفض أي منتج لا يحتوي كائن السعر الخاص به على `usdAmount` أو عملة USD صريحة. عندما ترجع SHEIN السعر المحلي فقط، كانت النتيجة أن المنتج يُستبعد بالكامل، وقد تنتهي المعاينة بصفر منتجات رغم أن السلة موجودة.

## الإصلاح

- ما زال `usdAmount` هو السعر المفضل متى كان موجودًا.
- إذا لم ترجع SHEIN `usdAmount`، لا يُحذف المنتج؛ يستخدم النظام السعر المحلي المتاح مع عملته الصحيحة.
- تُستنتج العملة الاحتياطية من `local_country` في رابط مشاركة SHEIN بدل إجبار كل النتائج على USD.
- لم يتم تغيير منطق الطلبات أو الدفع أو مراجعة الأصناف.

## اختبار الانحدار

أضيف اختبار Node يثبت أن منتج السلة لا يختفي عندما تحذف SHEIN `usdAmount` وتعيد سعرًا محليًا فقط.

كما تم تمرير حمولة SHEIN المحفوظة التي سبق أن احتوت 35 منتجًا عبر محلل النسخة المصححة، واستمر استخراج 35 منتجًا مع تفضيل USD عندما يكون `usdAmount` موجودًا.

## التحقق المنفذ في بيئة التجهيز

- Node syntax: PASS
- Node tests: 8/8 PASS
- Static test suite: PASS
- PHP syntax للملفات الرئيسية الخاصة بالاستيراد: PASS
- اختبار الحمولة المحفوظة ذات 35 منتجًا: PASS

لم يمكن تشغيل PHPUnit كاملًا في بيئة التجهيز لأن امتدادات PHP `dom`, `mbstring`, `xmlwriter` غير متوفرة فيها. كما أن الوصول المباشر إلى SHEIN من هذه البيئة محظور، لذلك يجب إجراء الاختبار الحي للرابط على جهاز التشغيل بعد فك النسخة.
