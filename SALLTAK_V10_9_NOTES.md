# Salltak V10.9 — قرارات الطلب والملاحظات المتراكمة

## ما تغير
- كل ملاحظة جديدة على الطلب تُحفظ كحركة مستقلة داخل `order_status_histories` ولا تستبدل الملاحظات السابقة.
- الرفض والإلغاء وحالة «يحتاج رد العميل» تتطلب سببًا إجباريًا من السيرفر.
- هذه الأسباب تكون ظاهرة للعميل تلقائيًا وتظهر في Timeline الطلب.
- قبول الطلب يدعم «ملاحظة القبول للعميل» بشكل اختياري، وتُحفظ مع حركة الاعتماد.
- تغييرات حالة الطلب تستخدم `visibility=auto`: حالة الطلب رحلة مرئية للعميل؛ الملاحظات التشغيلية المستقلة تبقى منفصلة ويمكن تحديدها داخلية أو للعميل.
- العميل يتلقى Notification داخل النظام على التحديثات الظاهرة له.
- تمت إضافة خيار «إشعارات العميل بحالة الطلب» في إعدادات النظام لإرسال بريد للعميل عند قرار/حالة/ملاحظة ظاهرة له.
- فشل البريد لا يوقف تحديث الطلب؛ يتم تسجيل التحذير فقط.

## قاعدة البيانات
لا توجد Migration جديدة في V10.9؛ النظام يستخدم سجل الحالات الموجود من V10.5.

## بعد التحديث
```bash
php artisan optimize:clear
php artisan test
```
