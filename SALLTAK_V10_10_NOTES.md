# سلتك V10.10 — ملاحظات الإصدار

## دورة السلة والطلب

- السلة تبدأ بحالة **محفوظة**.
- عند إرسالها للمراجعة تتحول إلى **تم إرسال الطلب**.
- كل سلة ترتبط بطلب واحد فقط.
- إنشاء الطلب يتم داخل Transaction مع `lockForUpdate()` على السلة لمنع تكرار الطلب عند الضغط السريع أو الطلبات المتزامنة.
- إذا حاول العميل إرسال السلة مرة أخرى، يفتح النظام الطلب المرتبط بدل إنشاء طلب جديد.
- لا يمكن للعميل حذف أو إلغاء سلة بعد تحويلها إلى طلب، لأن سجل الطلب يجب أن يبقى محفوظًا.
- السلات القديمة التي لديها طلب مرتبط تتم مزامنة حالتها إلى **تم إرسال الطلب** عند محاولة فتح دورة الإرسال مرة أخرى.

## LYPay والدفع النقدي التجريبي

- كتالوج الدفع نفسه يظل آمنًا: طرقه تُنشأ غير مفعلة عند التثبيت المباشر.
- في بيئة `local` و`testing` فقط، يقوم `DemoPaymentMethodsSeeder` بتفعيل:
  - **LYPay** ببيانات وهمية واضحة وغير صالحة للدفع الحقيقي.
  - **الدفع النقدي** مع السماح بالعربون والرصيد.
- يمكن إيقاف بيانات الدفع التجريبية عبر `DEMO_PAYMENT_METHODS=false`.
- في بيئة `production` لا تُحقن بيانات LYPay التجريبية تلقائيًا.
- إيقاف LYPay أو الدفع النقدي من لوحة التحكم يخفي الطريقة عن العميل لأن واجهة العميل تعرض الطرق المفعلة والمتاحة فقط.

## اللغة

- النصوص الجديدة في هذا الإصدار مكتوبة باللغة العربية الفصحى الواضحة.
