# إصلاح استيراد سلة SHEIN — V10.12.2

## السبب

المستورد كان يعتمد على أن صفحة مشاركة SHEIN تستدعي تلقائيًا واجهة `bff-api/order/cart/share/landing` أثناء تحميل الصفحة. عندما تفتح الصفحة دون أن ترسل هذا الطلب، تكون النتيجة صفر منتجات رغم أن `group_id` صحيح.

## الإصلاح

- استخراج `group_id` و`local_country` من رابط المشاركة.
- بعد فتح الرابط داخل جلسة Chromium نفسها، يستدعي العامل مباشرة واجهة سلة SHEIN:
  `/{locale}/bff-api/order/cart/share/landing?_ver=1.1.8&_lang={language}`
- يرسل الجسم الرسمي المستخدم في الاختبارات السابقة: `groupId`, `localCountry`, `userLocalSizeCountry`.
- تتم معالجة الاستجابة بنفس مرشح منتجات السلة لمنع التوصيات والإعلانات.
- يبقى مسار Network/State/DOM القديم كمسارات احتياطية.
- لم يتم تغيير منطق الطلبات أو الدفع أو مراجعة الأصناف.

## التحقق

- اختبار RED أثبت أن النسخة السابقة لا تبني ولا تستدعي طلب BFF المباشر.
- بعد الإصلاح يمر اختبار بناء واستدعاء BFF المباشر.
- جميع اختبارات Node وStatic: 36/36 PASS.
- `node --check` لعامل Playwright: PASS.
- `php -l` لملفات الاستيراد الرئيسية: PASS.

تعذر الاختبار الحي لـSHEIN داخل بيئة التجهيز لأن الشبكة تمنع الموقع برسالة `ERR_BLOCKED_BY_ADMINISTRATOR`، لذلك يجب إجراء الاختبار الحي على جهاز التشغيل.
