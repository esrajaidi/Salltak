# Salltak V10.11.1

إصلاح عاجل لصفحة تفاصيل الطلب:

- إصلاح `ParseError: syntax error, unexpected end of file` في `resources/views/orders/show.blade.php`.
- السبب كان وجود كتلة Blade `@php ... @endphp` بعد أوامر `@php(...)` داخل نفس القالب.
- نُقلت متغيرات سياسة إلغاء الطلب إلى كتلة PHP الأساسية أعلى الملف.
- لم يتغير منطق الإلغاء أو العربون أو الدفع أو الأسعار.
- أضيف فحص انحدار يمنع إعادة هذا النمط في صفحة تفاصيل الطلب.
